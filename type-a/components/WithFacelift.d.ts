import { ReactNode } from 'react';
declare type WithThemedPreviewProps = {
    children: ReactNode;
};
export declare const WithFacelift: ({ children }: WithThemedPreviewProps) => JSX.Element;
export {};
